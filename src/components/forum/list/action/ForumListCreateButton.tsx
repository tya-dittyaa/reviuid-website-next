import { Button } from "antd";

const ForumListCreateButton: React.FC = () => {
  return (
    <Button
      type="primary"
      size="large"
      style={{
        width: "auto",
        color: "black",
      }}
    >
      Buat Forum
    </Button>
  );
};

export default ForumListCreateButton;
